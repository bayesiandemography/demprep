
date_to_triangle <- function(date, dob,
                             min = 0, max = 100, width = 1,
                             open_right = TRUE,
                             first_day = "1 January", year_to = TRUE,
                             as_factor = TRUE) {
}

date_to_triangle_month <- function(date, dob,
                                   min = 0, max = 1200,
                                   open_right = TRUE,
                                   as_factor = TRUE) {
}

date_to_triangle_quarter <- function(date, dob,
                                     min = 0, max = 400,
                                     open_right = TRUE,
                                     as_factor = TRUE) {
}


